function toggleMenu() {
  const navList = document.querySelector(".nav-list");
  navList.classList.toggle("show");
  /*var currentOpacity = parseFloat(window.getComputedStyle(navList).opacity);
  navbar.style.opacity = navbar.style.opacity === "0" ? "1" : "0";*/
}
